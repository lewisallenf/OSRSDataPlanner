CREATE TABLE character(
    id               BIGSERIAL GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    username        VARCHAR NOT NULL,
    combatLevel     DOUBLE PRECISION NOT NULL,
    totalXP         DOUBLE PRECISION NOT NULL,
    requestCreated  TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
)